version: '3.8'
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: context_db
      POSTGRES_USER: admin
    volumes:
      - ./db/schema.sql:/docker-entrypoint-initdb.d/init.sql
